name: The most simple action ever

on:
  push:
    branches:
      - main

jobs:
  print-stuff:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Actions are amazing"
      - run: echo "Hello I am the first action"
